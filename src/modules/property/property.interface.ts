export interface IPropertyQuery {
  //post model er fields
  // title ?: string;
  // content ?: string

  searchTerm?: string;
  page?: string;
  limit?: string;
  sortOrder?: string;
  sortBy?: string;
}
